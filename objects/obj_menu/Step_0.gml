draw_set_font(font1);
draw_set_halign(fa_center);
draw_set_valign(fa_middle);

// Ajuste a posição do título
var x_pos = display_get_width() / 2;
var y_pos = display_get_height() / 4;

draw_text(x_pos, y_pos, "Criptomundo: O Último Herói");
